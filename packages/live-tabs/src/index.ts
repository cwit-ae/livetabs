// live-tabs — convenience umbrella. `npm i live-tabs` gives the router-agnostic
// core plus the flagship TanStack Router adapter (keep-alive included), so
// single-router apps never touch the @live-tabs/* scopes.
//
// Using another router? Install @live-tabs/<router> directly instead.
export * from "@live-tabs/tanstack-router"
