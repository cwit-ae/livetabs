// livetabs — convenience umbrella. `npm i livetabs` gives the router-agnostic
// core plus the flagship TanStack Router adapter (keep-alive included), so
// single-router apps never touch the @livetabs/* scopes.
//
// Using another router? Install @livetabs/<router> directly instead.
export * from "@livetabs/tanstack-router"
